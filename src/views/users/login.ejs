<!DOCTYPE html>

<html lang="en">
  <head>
    <title>Home | DeCamping.com</title>
    <%- include("../partials/head.ejs") %>
    <script defer src="/js/validations/login-validation.js" ></script>

  </head>

  <body>
    <!-- Header section (fixed) -->
    <%- include("../partials/header.ejs") %>

    <!-- Main section -->
    <main>
      <h1 class="text-titles login-register">ACCESO USUARIO</h1>
      <ul class="product-validation-errors hidden"> </ul>

      <div class="register-wrapper">
        <div class="content-wrapper-1">
          <form class="register-form" action="/users/login" method="POST">
            <div class="form-div" id="emailDiv">
              <label class="form-label" for="email">Correo electrónico</label>
              <input type="text" name="email" placeholder="Ej: ejemplo@gmail.com" id="email"
              value="<%=locals.oldValues ? oldValues.email : "" %>" >
              <div id="email-error-msg">
                <% if (locals.errors && errors.email) { %>
                    <p><%= errors.email.msg %></p>
                  <% } %>
              </div>
            </div>
            <div class="form-div">
              <label class="form-label" for="password">Contraseña</label>
              <input type="password" name="password" placeholder="Ingrese su contraseña" id="password"/>
              <div id="password-error-msg">
                <% if (locals.errors && errors.password) { %>
                    <p><%= errors.password.msg %></p>
                <% } %>
                <% if (locals.errors && errors.credenciales) { %>
                    <p><%= errors.credenciales.msg %></p>
                <% } %>
              </div>
            </div>
            <div class="form-div">
              <label class="remember-me" for="remember_user"
                ><input
                  type="checkbox"
                  name="remember_user"
                  value="rememberUser"
                />Recordarme</label
              >
            </div>
            <div class="form-div">
              <button class="boton-ppal color-primario" type="submit" href="#">LOGIN</button>
            </div>
            <div class="form-div align-center">
              <a class="password-link" href="#">Olvidé mi contraseña</a>
            </div>
          </form>
          <hr class="simple-line" />
          <div class="login-footer">
            <i class="far fa-user login-icon"></i>
            <h3>Todavía no te registraste?</h3>
            <a href="/users/register"> <h2>CREAR UNA CUENTA</h2> </a>
            <hr class="decoration-line" />
          </div>
        </div>
        <div class="vertical-line-2"></div>
        <div class="content-wrapper-2">
          <div class="form-div">
            <i class="far fa-user login-icon"></i>
            <h3>Todavía no te registraste?</h3>
            <a href="/users/register"><h2>CREAR UNA CUENTA</h2></a>
            <hr class="decoration-line" />
          </div>
        </div>
      </div>
    </main>

    <!-- Footer section -->
    <%- include("../partials/footer.ejs") %>
  </body>
</html>
